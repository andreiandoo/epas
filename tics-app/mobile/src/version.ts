/* =========================================================
   Versiunea aplicatiei, dintr-un singur loc.

   Scrisa AICI, nu citita din `package.json`: pachetul are versiunea proiectului
   (0.1.0, nemodificata de la inceput), pe cand ce conteaza pentru utilizator e
   versiunea BUNDLE-ului pe care il are pe telefon — cea din manifestul OTA.
   Ecranul de setari afisa „v1.0.0" scris de mana, care n-a fost niciodata
   adevarat.

   Se actualizeaza odata cu `publish-bundle.ps1 -Version x.y.z`.
   ========================================================= */
export const APP_VERSION = 'v3.4.0';
