<?php
/**
 * Copiază acest fișier în `includes/config.local.php` (ne-versionat, ignorat la deploy)
 * și pune aici override-urile specifice serverului. Se încarcă DUPĂ config.php.
 *
 * Cel mai important: TENANT_ID trebuie să fie id-ul real al tenantului leisure „Nordvale”.
 */

// ID-ul tenantului de tip leisure. Nordvale = 36 pe staging.
if (!defined('TENANT_ID')) { define('TENANT_ID', 36); }

// Implicit config.php pointează pe STAGING (stage.tixello.com).
// Pentru a comuta pe PRODUCȚIE, decomentează:
// if (!defined('API_BASE')) { define('API_BASE', 'https://core.tixello.com/api'); }
// if (!defined('CORE_URL')) { define('CORE_URL', 'https://core.tixello.com'); }

// Afișează erorile de API în pagină pe timpul dezvoltării.
// if (!defined('DEBUG')) { define('DEBUG', true); }
